library(gstat)
library(sp)
library(raster)
setwd("C:\\SWAT\\ProgTest\\xgeswat\\Source\\original")
sample.dem <- raster('dem.tif')
plot(sample.dem)
agg.fact <- 6
agg.dem <- aggregate(sample.dem, fact = agg.fact, fun=mean)
sample.pts <- as(agg.dem, 'SpatialPointsDataFrame')
v.sample <- variogram(dem~1, sample.pts)
m.sample <- fit.variogram(v.sample, vgm(200000, "Exp", 200000, 1))
plot(v.sample, model = m.sample)
sample.grid <- as(sample.dem, 'SpatialPixelsDataFrame')
g.dummy <- gstat(formula = dem~1, dummy = TRUE, data = sample.pts, model =m.sample, nmax = 20, beta = 5)
saveRDS(sample.grid, "dem.Rds")
test.rds <- readRDS("dem.rds")
#yy.sample1 <- predict(g.dummy, sample.grid, nsim = 1)
#saveRDS(yy.sample1, "seq11.Rds")
#yy.sample2 <- predict(g.dummy, sample.grid, nsim = 1)
#saveRDS(yy.sample2, "seq12.Rds")
yy.sample3 <- predict(g.dummy, sample.grid, nsim = 600)
saveRDS(yy.sample3, "seq6001.Rds")
yy.sample4 <- predict(g.dummy, sample.grid, nsim = 700)
saveRDS(yy.sample4, "seq7002.Rds")
yy.sample3 <- predict(g.dummy, sample.grid, nsim = 600)
saveRDS(yy.sample3, "seq6003.Rds")
yy.sample4 <- predict(g.dummy, sample.grid, nsim = 700)
saveRDS(yy.sample4, "seq7004.Rds")
yy.sample3 <- predict(g.dummy, sample.grid, nsim = 600)
saveRDS(yy.sample3, "seq6005.Rds")
yy.sample4 <- predict(g.dummy, sample.grid, nsim = 700)
saveRDS(yy.sample4, "seq7006.Rds")
#yy.sample1 <- predict(g.dummy, sample.grid, nsim = 1)
#saveRDS(yy.sample1, "seq11.Rds")
#yy.sample2 <- predict(g.dummy, sample.grid, nsim = 1)
#saveRDS(yy.sample2, "seq12.Rds")
yy.sample3 <- predict(g.dummy, sample.grid, nsim = 600)
saveRDS(yy.sample3, "seq6007.Rds")
yy.sample4 <- predict(g.dummy, sample.grid, nsim = 700)
saveRDS(yy.sample4, "seq7008.Rds")
yy.sample3 <- predict(g.dummy, sample.grid, nsim = 600)
saveRDS(yy.sample3, "seq6009.Rds")
yy.sample4 <- predict(g.dummy, sample.grid, nsim = 700)
saveRDS(yy.sample4, "seq70010.Rds")
yy.sample5 <- predict(g.dummy, sample.grid, nsim = 600)
saveRDS(yy.sample5, "seq60011.Rds")
yy.sample6 <- predict(g.dummy, sample.grid, nsim = 700)
saveRDS(yy.sample6, "seq70012.Rds")
